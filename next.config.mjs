/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      's3-alpha-sig.figma.com',
      'ikf.onrender.com',
      'localhost',
    ],
  },
}

export default nextConfig
