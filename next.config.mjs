/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      's3-alpha-sig.figma.com',
      'ikf.onrender.com',
      'localhost',
      'example.com',
    ],
  },
}

export default nextConfig
